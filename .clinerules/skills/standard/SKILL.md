---
name: standard
description: Работа со стандартами реализации (`docs/standards/`) — создание, обновление, удаление. Используй когда нужно добавить новый стандарт, изменить существующий или удалить устаревший.
---

Перед началом — прочитать `ARCHITECTURE.md`.

## Путь

`docs/standards/<kebab-name>.md`. Нумерации нет — имя должно отражать суть.

## Создание стандарта

1. Создать файл `docs/standards/<kebab-name>.md` по шаблону `assets/standard-template.md`.
2. Применить шаг «Консистентность».

## Обновление стандарта

1. Обновить содержимое файла.
2. Применить шаг «Консистентность».

## Удаление стандарта

1. Удалить файл `docs/standards/<kebab-name>.md`.
2. Применить шаг «Консистентность».

## Консистентность

При любом изменении стандарта:

1. Обновить индекс стандартов в `ARCHITECTURE.md` (добавить/обновить/убрать строку).
2. Обновить перекрёстные ссылки в связанных ADR, AR, диаграммах.
3. Обновить `docs/architecture/overview.md`, если стандарт влияет на структуру системы.
4. Обновить `docs/architecture/components.md`, если стандарт влияет на компоненты.

## Принципы

- Стандарт — практическое требование к реализации (как делать).
- AR — архитектурное ограничение (что можно/нельзя).
- ADR — обоснование решения (почему).
- Стандарт ссылается на AR/ADR, не дублирует их.
