# Правила работы с Git

## Запреты

- **Никогда не делать `git push`** самостоятельно — только по явной команде оператора.
- **Никогда не делать `git commit`** самостоятельно — только по явной команде оператора.

## Commit

- При создании коммита использовать skill `git-commit`.
