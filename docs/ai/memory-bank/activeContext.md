# Active Context

## Текущая задача

Исправлен UI пейджера на странице ингредиентов.

## Что было сделано в последней сессии

- Исправлен `app/ingredients/page.tsx`: классы `paginator` → `pagination`, `paginator-btn` → `page-btn`.
  - Причина бага: компонент использовал несуществующие CSS-классы вместо готовых из дизайн-системы (`.pagination`, `.page-btn` в `globals.css`).
  - Результат: пейджер теперь отображается как красивые pill-кнопки, выровненные по центру.
