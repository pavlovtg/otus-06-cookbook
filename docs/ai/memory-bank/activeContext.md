# Active Context

## Текущая задача

Исправлен баг: при повторном открытии модала создания ингредиента поля были заблокированы (кнопка в состоянии "Сохранение...").

## Что было сделано в последней сессии

- Исправлен `IngredientModal.tsx`: добавлен `setLoading(false)` после `setOpen(false)` в `handleSubmit` (успешный путь).
  - Причина бага: при успешном сохранении `loading` не сбрасывался, компонент не размонтируется при `router.refresh()`, поэтому стейт сохранялся между открытиями.
