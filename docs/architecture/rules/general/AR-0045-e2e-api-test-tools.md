# AR-0045: Инструменты E2E API тестов

Источник: ADR-0033

## Правило

- E2E API тесты реализуются на pytest + httpx.
- Тесты размещаются в `tests/e2e/`.
- Сценарии берутся из `openspec/specs/`; покрывают happy path и критичные ошибки.
- Тесты запускаются против запущенного `docker compose up`.
