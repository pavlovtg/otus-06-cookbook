# AR-0038: Один тип — один файл

## Правило

Каждый тип (класс, интерфейс, enum, record, struct) размещается в отдельном файле. Имя файла совпадает с именем типа.

## Запрещено

- Объявлять несколько типов в одном `.cs`-файле.
- Использовать вложенные публичные типы как замену отдельным файлам.
